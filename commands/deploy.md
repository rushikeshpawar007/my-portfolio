---
name: deploy
description: Run the full deployment workflow — build, test, validate, and push
---

You MUST follow the portfolio-deploy skill before executing any deployment steps.

Load and execute the `portfolio-deploy` skill now. Do NOT skip any validation gates.

After all gates pass, summarize what was validated and confirm the push.
